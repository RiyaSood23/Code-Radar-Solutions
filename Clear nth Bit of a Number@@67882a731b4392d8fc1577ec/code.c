#include <stdio.h>

char* welcome() {
    return "Welcome to Code Radar!";
}

int main() {
    int a,b;
    scanf("%d %d",&a,&b);
    // int bit=(a>>b)&0;
    // printf("%d",bit);
    int mask=1<<(i-1);
    x=x|mask;
    printf("%d",x);
    return 0;
}